# Copy this file to terraform.tfvars and fill in your values

# Required variables
database_name = "my-astra-db"
keyspace_name = "mykeyspace"
cloud_provider = "AWS"
regions = ["us-east-1"]

# Optional variables (uncomment and modify as needed)
database_name = "my-astra-db"
keyspace_name = "mykeyspace"
cloud_provider = "AWS"
regions = ["us-east-1"]

# Astra API Token (preferably set via environment variable ASTRA_API_TOKEN or use HashiCorp Vault)
# astra_token = "your-astra-api-token"
